# Changelog

All notable changes follow [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Added

- **Initial contract implementation**: the chip spec (`spec/chip-spec.md`),
  the `chip` Python package (validation + conformance kit), example chips,
  and a conformance test suite. Defines the vocabulary of a chip — ports,
  stages, circuits, bindings, receipts — and the envelope a host uses to
  run one.
