{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/lavallee/chip/tree/main/examples/publication-attention/schemas/quiet-run.json@1",
  "title": "Quiet run (v1)",
  "description": "A first-class successful no-finding result (spec 9). Records only the attention semantics needed to stay quiet next time.",
  "type": "object",
  "additionalProperties": true,
  "required": ["kind", "reason"],
  "properties": {
    "kind": { "const": "quiet" },
    "reason": {
      "type": "string",
      "description": "Why the run was quiet, e.g. 'already-seen', 'duplicate-content', 'not-after-cursor'."
    },
    "lineage": {
      "type": "object",
      "additionalProperties": true,
      "description": "Passthrough of the originating signal lineage, when present."
    },
    "producedBy": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "chip": { "type": "string" },
        "run": { "type": "string" },
        "circuit": { "type": "string" }
      }
    }
  }
}
